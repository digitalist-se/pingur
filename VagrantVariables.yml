---
wk_box:
  ip: 192.168.10.10
  hostname: pingur # Cannot contain any special characters.
  memory: 2048
  server:
    hostnames: pinguur.dev
    hostname: pingur.dev
    webroot: /srv/www/pingur
    database:
      name: pingur
      user: root
      password: password
      host: localhost
      port: 3306
  sync_folder:
    host: ./
    guest: /srv/www/pingur
